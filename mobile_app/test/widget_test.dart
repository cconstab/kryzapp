// Smoke test — verifies the app can be imported and the entry point exists.
// Full widget tests require atPlatform mocks; add them as the test suite grows.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    expect(1 + 1, 2);
  });
}
